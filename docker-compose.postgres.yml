services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: findit
      POSTGRES_PASSWORD: findit_dev
      POSTGRES_DB: findit
    ports:
      - "5432:5432"
    volumes:
      - findit_pgdata:/var/lib/postgresql/data

volumes:
  findit_pgdata:
