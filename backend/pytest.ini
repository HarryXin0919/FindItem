[pytest]
# Test bootstrap: make `app` importable no matter which directory pytest is
# started from, so `pytest` at the repo root behaves like `cd 09_Code/backend
# && pytest`.
pythonpath = .
testpaths = tests
addopts = -q
